# Masters in John Paul II and Leadership

This repository contains the static site for the Masters in John Paul II and Leadership website.

## GitHub Pages setup

The site is configured for GitHub Pages deployment via GitHub Actions.

- The site source is the repository root.
- The workflow is located at `.github/workflows/pages.yml`.
- When you push to the `main` branch, GitHub Actions will publish the site automatically.

## Local preview

Open `index.html` in your browser or use a local server such as `python -m http.server`.
